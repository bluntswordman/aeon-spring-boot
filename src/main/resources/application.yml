spring:
  datasource:
    url: jdbc:postgresql://aeon-db-server.postgres.database.azure.com:5432/aeon_db
    username: azureaeonadmin@aeon-db-server
    password: @eoN1015
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  main:
    web-application-type: servlet